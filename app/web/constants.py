# Page size caps for web UI views.
# These are intentional UX defaults — not env-configurable — that keep
# individual pages readable without requiring server-side pagination controls.
WEB_HOME_TOP_LEADS: int = 5
WEB_HOME_RECENT_RUNS: int = 5
WEB_CAMPAIGNS_LIST_LIMIT: int = 50
WEB_BUSINESSES_LIST_LIMIT: int = 100
WEB_LEADS_LIST_SIZE: int = 100
